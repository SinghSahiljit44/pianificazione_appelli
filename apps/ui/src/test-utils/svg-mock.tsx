// Stub per gli import SVG via svgr (`*.svg?react`) sotto jest, che non applica
// la trasformazione di vite-plugin-svgr. Esporta un componente vuoto.
export default function SvgMock() {
  return null;
}
